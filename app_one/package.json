{
  "name": "app_one",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prod": "npm run build && serve -s dist -l 5281 --cors"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.4.1",
    "serve": "^14.2.4",
    "vite": "^7.2.4"
  }
}
